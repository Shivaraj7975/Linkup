# Workspace Behavioral Rules & Directives

## Git & GitHub Push Control
- **NEVER execute `git push` or push code to GitHub automatically under any circumstances**.
- Always keep all code modifications, refactorings, builds, and tests strictly local.
- Ask for explicit user confirmation before running any git push operation.
